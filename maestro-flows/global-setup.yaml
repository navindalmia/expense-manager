---
# Global Setup: Cleanup test data
appId: com.navindalmia.expensemanager
name: GlobalSetup
---
- launchApp

# Clear all test users from database (via internal API)
- evalScript: |
    const response = http.delete('http://localhost:4000/api/internal/clear-all-test-users');
    output.cleanupStatus = response.status;
