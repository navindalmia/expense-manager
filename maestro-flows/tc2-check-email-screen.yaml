---
# TC2: CheckEmailScreen displays email and resend button
appId: com.navindalmia.expensemanager
name: TC2 - CheckEmailScreen UI Elements
---
- launchApp

# Generate unique test email
- evalScript: ${output.testEmail = 'e2e-check-' + Date.now() + '@example.com'}

# Signup
- runFlow:
    file: shared-signup-flow.yaml
    env:
      email: ${output.testEmail}
      password: "TestPass123!"
      name: "Test User"

# Verify email is displayed
- assertVisible:
    text: ${output.testEmail}

# Verify resend button
- assertVisible:
    text: "Resend"
    id: resend-button

# Verify helper text
- assertVisible:
    text: "check your email"

# DATABASE: Verify user created and unverified
- runFlow:
    file: shared-verify-email-state.yaml
    env:
      email: ${output.testEmail}
      expectedEmailVerified: false
      expectedTokenIsUsed: false
