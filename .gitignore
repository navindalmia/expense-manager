# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files (personal overrides only — .env.development is the committed
# shared default and is intentionally NOT matched by these patterns)
.env*.local
frontend/.env

# typescript
*.tsbuildinfo

# test coverage reports
coverage/

# generated native folders (root-level, and frontend/ where expo prebuild
# actually generates them — the /pattern above only anchors to repo root)
/ios
/android
frontend/ios
frontend/android

# Development rules (not for git)
.cursorrules

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# ============================================================================
# MARKDOWN FILE RULES (See WORKFLOW.md > CORE PRINCIPLE)
# ============================================================================
# ALLOWED IN REPO: Configuration, project state, onboarding (.instructions.md, WORKFLOW.md, PROJECT_MEMORY/*, etc.)
# NEVER COMMIT: Ephemeral work (reviews, checkpoints, plans, session notes)
# Reason: Once decisions are made, code is truth. Ephemeral files clutter history.
# Exceptions handled by .gitignore patterns below:
# ============================================================================

# Ephemeral docs - never commit (see WORKFLOW.md > CORE PRINCIPLE)
# Review documents
*_REVIEW*.md
*_CODE_REVIEW*.md

# Checkpoint & session files
*_CHECKPOINT*.md
*_SESSION*.md
SESSION_*.md

# Planning documents
*_PLAN*.md
*_ROADMAP*.md
*_PROPOSAL*.md

# Test reports and output
TEST_RESULTS*.md
TEST_OUTPUT*.md

# Other discussions and personal documents
other discussions/

